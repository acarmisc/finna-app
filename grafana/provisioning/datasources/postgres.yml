apiVersion: 1

datasources:
  - name: finops-pg
    type: postgres
    url: postgres:5432
    database: finops
    user: finops
    secureJsonData:
      password: finops_dev
    jsonData:
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2
      connMaxLifetime: 14400
      postgresVersion: 1600
    isDefault: true
    editable: true