# Apply this manually: kubectl create secret generic finops-secrets \
#   --namespace finops \
#   --from-literal=PG_DSN=<your-dev-dsn> \
#   --from-literal=JWT_SECRET=<your-dev-jwt-secret> \
#   --from-literal=GCP_PROJECT_ID=<your-dev-gcp-project-id> \
#   --from-literal=GCP_SERVICE_ACCOUNT_KEY=<your-dev-gcp-service-account-json-base64> \
#   --from-literal=AZURE_SUBSCRIPTION_ID=<your-dev-azure-subscription-id> \
#   --from-literal=AZURE_TENANT_ID=<your-dev-azure-tenant-id> \
#   --from-literal=AZURE_CLIENT_ID=<your-dev-azure-client-id> \
#   --from-literal=AZURE_CLIENT_SECRET=<your-dev-azure-client-secret> \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: finops-secret
  namespace: finops
type: Opaque
data: {}  # Populated via manual kubectl
