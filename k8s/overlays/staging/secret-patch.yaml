# Apply this manually: kubectl create secret generic finops-secrets \
#   --namespace finops \
#   --from-literal=PG_DSN=<your-staging-dsn> \
#   --from-literal=JWT_SECRET=<your-staging-jwt-secret> \
#   --from-literal=GCP_PROJECT_ID=<your-staging-gcp-project-id> \
#   --from-literal=GCP_SERVICE_ACCOUNT_KEY=<your-staging-gcp-service-account-json-base64> \
#   --from-literal=AZURE_SUBSCRIPTION_ID=<your-staging-azure-subscription-id> \
#   --from-literal=AZURE_TENANT_ID=<your-staging-azure-tenant-id> \
#   --from-literal=AZURE_CLIENT_ID=<your-staging-azure-client-id> \
#   --from-literal=AZURE_CLIENT_SECRET=<your-staging-azure-client-secret> \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: finops-secret
  namespace: finops
type: Opaque
data: {}  # Populated via manual kubectl
